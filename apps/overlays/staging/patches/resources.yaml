apiVersion: apps/v1
kind: Deployment
metadata:
  name: planka
spec:
  template:
    spec:
      containers:
        - name: planka
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
