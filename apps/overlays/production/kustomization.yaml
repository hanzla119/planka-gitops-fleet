apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base/planka
  - ../../base/postgres
  - patches/ingress-alb.yaml

patches:
  - path: patches/replicas.yaml
